# Backend

FastAPI backend for Bookies.

## Run locally

Create environment:

python -m venv venv

Activate:

.\venv\Scripts\activate

Install:

pip install -r requirements.txt

Run:

uvicorn main:app --reload